syntax = "proto3";
package gordian.rpc.core;

option go_package = "github.com/cometbft/cometbft/rpc/core/types";

// This is just the shell of cometbft abci types as a hacky work around
// This allows the native types of the RPC
// to be used without having to import the entire cometbft package to our proto

message ResultTx {}

message ResultBroadcastTx {}

message ResultBlockSearch {}

message ResultCommit {}

message ResultABCIQuery {}
